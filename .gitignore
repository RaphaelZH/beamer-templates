*.aux
*.log
*.nav
*.out
*.snm
*.toc
*.vrb
*.synctex.gz
missfont.log
slides.pdf
.DS_Store

# The demo's own build artefacts. Its PDF is gitignored like any other; the
# screenshots in screenshots/ are committed, the raw page-NN.png dumps are not.
demo.pdf
**/screenshots/page-*.png

# Overleaf zips built by make-overleaf-zips.sh; attached to releases, not committed
dist/
